apiVersion: apps/v1
kind: Deployment
metadata:
  name: ion-deployment
  labels:
    app: ion
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ion
  template:
    metadata:
      labels:
        app: ion
    spec:
      containers:
      - name: ion
        image: ssadcloud/alpha:latest
        ports:
        - containerPort: 80
